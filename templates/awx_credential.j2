{% if item is defined%}
tower-cli credential create -h {{ awx_host }} -u {{  awx_username }} -p {{ awx_password }} --credential-type="{{ item.type }}" --name={{ item.name }} --inputs='{"username": "{{ item.name }}", "password": "{{ item.password }}"}'
{% endif %}
